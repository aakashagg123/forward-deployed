# Getting started

To add a page:

1. Create a markdown file anywhere under this space's folder.
2. Add a line linking to it in this space's `SUMMARY.md`, nested under a heading if it belongs in a section.
3. Push to `main` — the site rebuilds and redeploys automatically.
